"""
SafePush utility functions.

This package provides shared utilities used across the SafePush codebase.
Utilities are kept intentionally small and focused — if a utility grows complex
enough to warrant its own domain, it belongs in a dedicated package instead.

Available utilities:

* :mod:`safepush.utils.fs` — filesystem helpers
* :mod:`safepush.utils.git` — Git interaction helpers
* :mod:`safepush.utils.logging` — logging configuration
"""
